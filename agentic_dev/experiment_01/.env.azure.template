# Azure MCP Server - Environment Template
# Copy this file to .env.azure and fill in your Azure credentials

# ============================================
# Azure Service Principal Credentials
# ============================================
# These are obtained when you create a service principal:
# az ad sp create-for-rbac --name "xpr-mcp-server"

# Your Azure AD tenant ID
AZURE_TENANT_ID=

# Service principal application (client) ID
AZURE_CLIENT_ID=

# Service principal client secret (password)
AZURE_CLIENT_SECRET=

# Your Azure subscription ID
AZURE_SUBSCRIPTION_ID=

# ============================================
# Azure Resources
# ============================================

# Resource group where resources will be created/managed
AZURE_RESOURCE_GROUP=

# Key Vault name (for storing secrets)
AZURE_KEY_VAULT_NAME=

# Storage account name (for blob storage)
AZURE_STORAGE_ACCOUNT_NAME=

# Storage account access key
AZURE_STORAGE_ACCOUNT_KEY=

# ============================================
# Optional: Server Configuration
# ============================================

# Enable debug logging for Azure operations
AZURE_DEBUG=false

# Token cache duration (seconds, default: 1800)
AZURE_TOKEN_CACHE_TTL=1800

# API timeout (milliseconds, default: 30000)
AZURE_API_TIMEOUT=30000

# ============================================
# Instructions
# ============================================
# 1. Create a service principal:
#    az ad sp create-for-rbac --name "xpr-mcp-server"
#
# 2. Create a resource group:
#    az group create --name your-group --location eastus
#
# 3. Create a Key Vault:
#    az keyvault create --name your-vault --resource-group your-group
#
# 4. Create a storage account:
#    az storage account create --name youraccount --resource-group your-group
#
# 5. Grant permissions to service principal:
#    az keyvault set-policy --name your-vault --object-id <sp-object-id> \
#      --secret-permissions get list set delete
#
# 6. Update this file with your values
#
# 7. Load environment:
#    source .env.azure
#    npm start
